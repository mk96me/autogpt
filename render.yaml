services:
  - type: web
    name: autogpt-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn autogpt_platform.backend.backend.app:app --host 0.0.0.0 --port $PORT
